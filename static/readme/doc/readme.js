export default {
	
}
/**
 * 过滤器使用
 * newList(state){
 *		return state.list.filter(v=>{
 * 			return v.status
 *		})
 * 		也可以写成
 * 		return state.list.filter( v => v.status)
 * } 这样写法表示 在 state.list 集合中 查找 list 集合下对象里字段是 status 为 true 的对象并返回给 newList;
 * 
 * 
 */
